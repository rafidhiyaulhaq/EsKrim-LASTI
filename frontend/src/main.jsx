import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./index.css"; // Untuk Tailwind CSS jika Anda menggunakan file CSS khusus

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
